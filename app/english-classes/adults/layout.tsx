import { seoPages } from '@/lib/seo-pages'

export const metadata = seoPages.englishAdults

export default function EnglishAdultsLayout({ children }: { children: React.ReactNode }) {
  return children
}
