import { seoPages } from '@/lib/seo-pages'

export const metadata = seoPages.englishPrivateStudents

export default function EnglishPrivateStudentsLayout({ children }: { children: React.ReactNode }) {
  return children
}
