import { seoPages } from '@/lib/seo-pages'

export const metadata = seoPages.englishGovtStudents

export default function EnglishGovtStudentsLayout({ children }: { children: React.ReactNode }) {
  return children
}
