import { seoPages } from '@/lib/seo-pages'

export const metadata = seoPages.skilledMigrationCanada

export default function SkilledMigrationCanadaLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
