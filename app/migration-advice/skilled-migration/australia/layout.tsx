import { seoPages } from '@/lib/seo-pages'

export const metadata = seoPages.skilledMigrationAustralia

export default function SkilledMigrationAustraliaLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
