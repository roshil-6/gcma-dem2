import { seoPages } from '@/lib/seo-pages'

export const metadata = seoPages.uaeJobSearchTravel

export default function UaeJobSearchTravelLayout({ children }: { children: React.ReactNode }) {
  return children
}
